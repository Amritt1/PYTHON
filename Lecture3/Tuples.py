# A built-in data types that lets us create immutable sequences of values


tup= (21, 23 , 45, 67, 88,)  # tup[0], tup[1]

print(tup[0])
print(tup[1])

tup[0]= 5 # its not allowed in tuple

